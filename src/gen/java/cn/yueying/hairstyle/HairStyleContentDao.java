package cn.yueying.hairstyle;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;
import de.greenrobot.dao.query.Query;
import de.greenrobot.dao.query.QueryBuilder;

import cn.yueying.hairstyle.HairStyleContent;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table HAIR_STYLE_CONTENT.
*/
public class HairStyleContentDao extends AbstractDao<HairStyleContent, Long> {

    public static final String TABLENAME = "HAIR_STYLE_CONTENT";

    /**
     * Properties of entity HairStyleContent.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property StyleId = new Property(1, long.class, "styleId", false, "STYLE_ID");
        public final static Property PicUrl = new Property(2, String.class, "picUrl", false, "PIC_URL");
        public final static Property ContentDesc = new Property(3, String.class, "contentDesc", false, "CONTENT_DESC");
    };

    private DaoSession daoSession;

    private Query<HairStyleContent> hairStyle_HairStyleContentListQuery;

    public HairStyleContentDao(DaoConfig config) {
        super(config);
    }
    
    public HairStyleContentDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "'HAIR_STYLE_CONTENT' (" + //
                "'_id' INTEGER PRIMARY KEY ," + // 0: id
                "'STYLE_ID' INTEGER NOT NULL ," + // 1: styleId
                "'PIC_URL' TEXT," + // 2: picUrl
                "'CONTENT_DESC' TEXT);"); // 3: contentDesc
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "'HAIR_STYLE_CONTENT'";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, HairStyleContent entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getStyleId());
 
        String picUrl = entity.getPicUrl();
        if (picUrl != null) {
            stmt.bindString(3, picUrl);
        }
 
        String contentDesc = entity.getContentDesc();
        if (contentDesc != null) {
            stmt.bindString(4, contentDesc);
        }
    }

    @Override
    protected void attachEntity(HairStyleContent entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public HairStyleContent readEntity(Cursor cursor, int offset) {
        HairStyleContent entity = new HairStyleContent( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getLong(offset + 1), // styleId
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // picUrl
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3) // contentDesc
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, HairStyleContent entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setStyleId(cursor.getLong(offset + 1));
        entity.setPicUrl(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setContentDesc(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(HairStyleContent entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(HairStyleContent entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    /** Internal query to resolve the "hairStyleContentList" to-many relationship of HairStyle. */
    public List<HairStyleContent> _queryHairStyle_HairStyleContentList(long styleId) {
        synchronized (this) {
            if (hairStyle_HairStyleContentListQuery == null) {
                QueryBuilder<HairStyleContent> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.StyleId.eq(null));
                queryBuilder.orderRaw("STYLE_ID ASC");
                hairStyle_HairStyleContentListQuery = queryBuilder.build();
            }
        }
        Query<HairStyleContent> query = hairStyle_HairStyleContentListQuery.forCurrentThread();
        query.setParameter(0, styleId);
        return query.list();
    }

    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getHairStyleDao().getAllColumns());
            builder.append(" FROM HAIR_STYLE_CONTENT T");
            builder.append(" LEFT JOIN HAIR_STYLE T0 ON T.'STYLE_ID'=T0.'_id'");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected HairStyleContent loadCurrentDeep(Cursor cursor, boolean lock) {
        HairStyleContent entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        HairStyle hairStyle = loadCurrentOther(daoSession.getHairStyleDao(), cursor, offset);
         if(hairStyle != null) {
            entity.setHairStyle(hairStyle);
        }

        return entity;    
    }

    public HairStyleContent loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<HairStyleContent> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<HairStyleContent> list = new ArrayList<HairStyleContent>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<HairStyleContent> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<HairStyleContent> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
